name: flutter_nuevo
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.5
  cloud_firestore: ^4.15.5
  flutter_datetime_picker_plus: ^2.0.1  # ✅ Este reemplaza el plugin viejo
  google_maps_flutter: ^2.5.0           # ✅ Asegúrate de usar versión moderna
  geolocator: ^10.1.0                   # ✅ Actualizado
  flutter_application_1: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true